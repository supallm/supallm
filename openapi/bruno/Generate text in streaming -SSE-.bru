meta {
  name: Generate text in streaming -SSE-
  type: http
  seq: 4
}

post {
  url: /projects/:projectId/streamText
  body: json
  auth: bearer
}

params:path {
  projectId: 
}

auth:bearer {
  token: {{token}}
}
