meta {
  name: Get a credential by ID
  type: http
  seq: 3
}

get {
  url: {{baseURL}}/projects/{{projectId}}/credentials/{{credentialId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
