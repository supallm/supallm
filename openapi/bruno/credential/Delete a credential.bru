meta {
  name: Delete a credential
  type: http
  seq: 5
}

delete {
  url: {{baseURL}}/projects/{{projectId}}/credentials/{{credentialId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
