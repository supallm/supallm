meta {
  name: Delete a credential
  type: http
  seq: 5
}

delete {
  url: {{baseURL}}/projects/{{projectId}}/:credentialId
  body: none
  auth: bearer
}

params:path {
  credentialId: 
}

auth:bearer {
  token: {{token}}
}
