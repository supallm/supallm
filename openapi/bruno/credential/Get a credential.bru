meta {
  name: Get a credential
  type: http
  seq: 4
}

get {
  url: {{baseURL}}/projects/{{projectId}}/credentials/{{credentialId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
