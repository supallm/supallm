meta {
  name: Delete a workflow
  type: http
  seq: 5
}

delete {
  url: {{baseURL}}/projects/{{projectId}}/workflows/{{workflowId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
