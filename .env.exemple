# Clerk Config
CLERK_PUBLISHABLE_KEY=change-me
CLERK_SECRET_KEY=change-me

# Secret Key for encryption
# openssl rand -base64 32
SECRET_KEY=change-me

# OpenAI Config
# Deprecated (will be removed before releasing next major)
OPENAI_API_KEY=change-me

# PostgreSQL Config
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=supallm
POSTGRES_HOST=supallm-pg
POSTGRES_PORT=5432

# HTTP Config
FRONTEND_PORT=3000
BACKEND_PORT=3001

# Redis Config
REDIS_HOST=supallm-redis
REDIS_PORT=6379
REDIS_PASSWORD=redis